<%= form_for [topic, post, post.comments.new] do |f| %>
  <%= f.label :comment %>
  <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter post title" %>

    <div class="form-group">
       <%= f.submit "Save", class: 'btn btn-success' %>
    </div>
<% end %>
 