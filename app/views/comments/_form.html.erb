<%= form_for [@post, Comment.new], remote: true do |f| %>
  <%= f.label :commentaaaaa %>
  <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter comment here" %>

    <div class="form-group">
       <%= f.submit "Save", class: 'btn btn-success' %>
    </div>
<% end %>
 