<h1>New Summary</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for summaries:</p>
    <ul>
      <li>They should summarize the associated post.</li>
      <li>They shouldn't use profanity or sports references. </li>
      <li>They should be written in third person omniscient. </li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @summary do |s| %>
      <div class="form-group">
        <%= s.label :name %>
        <%= s.text_field :name, class: "form-control", placeholder: "Enter summary name" %>
      </div>
      <div class="form-group">
        <%= s.label :description %>
        <%= s.text_area :description, rows: 8, class: "form-control", placeholder: "Enter summary description" %>
      </div>
      <%= s.submit "Save", class: "btn btn-success" %>
    <% end %>
  </div>
</div>


